# Gateway
GATEWAY_PORT=3100
BLOCK_STORE_PATH=./data/blocks
HOST_PUBKEY=  # This gateway operator's Ed25519 public key (hex)
MINT_URL=http://localhost:3101

# L402 / Lightning (REST API, typically port 8080)
LND_HOST=localhost:8080
LND_MACAROON_PATH=./data/lnd/admin.macaroon
LND_TLS_CERT_PATH=./data/lnd/tls.cert

# Mint
MINT_PORT=3101
MINT_PRIVATE_KEY_HEX=  # Ed25519 private key (32 bytes hex)

# Coordinator
COORDINATOR_PORT=3102
DATABASE_URL=postgresql://dupenet:dupenet@localhost:5432/dupenet

# Node Agent
AGENT_GATEWAY_URL=http://localhost:3100
AGENT_COORDINATOR_URL=http://localhost:3102
AGENT_HOST_PUBKEY=  # This node's Ed25519 public key

# Pricing defaults
MIN_REQUEST_SATS=3
SATS_PER_GB=500

# Epoch
EPOCH_LENGTH_HOURS=4
