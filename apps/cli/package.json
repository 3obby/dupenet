{
  "name": "@dupenet/cli",
  "version": "0.0.1",
  "description": "CLI client — upload, fetch, tip, pin via the dupenet protocol",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "dupenet": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dupenet/physics": "*",
    "@dupenet/lnd-client": "*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.19.2",
    "@types/node": "^20.11.0"
  }
}
